services:
  kubevirt-provider-db:
    container_name: kubevirt-provider-db
    image: ${PGSQL_IMAGE:-quay.io/sclorg/postgresql-15-c9s:latest}
    environment:
      - POSTGRESQL_DATABASE=kubevirt-provider
      - POSTGRESQL_USER=admin
      - POSTGRESQL_PASSWORD=adminpass
      - POSTGRESQL_MASTER_USER=admin
      - POSTGRESQL_MASTER_PASSWORD=adminpass
      - PGPASSWORD=adminpass
    ports:
      - "5432:5432"
    volumes:
      - kubevirt-provider-db:/var/lib/pgsql/data
    networks:
      - kubevirt-provider-network
    restart: unless-stopped
volumes:
  kubevirt-provider-db:
    driver_opts:
      type: "tmpfs"  
      device: "tmpfs" 
      o: "nodev,noexec"
networks:
  kubevirt-provider-network:
